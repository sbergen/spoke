---
version: 1.2.7
title: First packet from server MUST be ConnAck
---
  --> Perform(Connect(ConnectOptions(False, "my-client", 15, None, None)))
<--   [OpenTransport]
  --> TransportEstablished
<--   [SendData(Connect(ConnectOptions(False, "my-client", 15, None, None)))]
  --> ReceivedPacket(PingResp)
<--   [CloseTransport, Publish(ConnectionStateChanged(DisconnectedUnexpectedly("The first packet sent from the Server to the Client MUST be a CONNACK Packet")))]