---
version: 1.2.7
title: QoS2 republish after reconnect
---
<flushed connect and handshake>
  --> PublishMessage(PublishData("topic", "payload", ExactlyOnce, False))
<--   SendData(Publish(PublishDataQoS2(MessageData("topic", "payload", False), False, 1)))
  --> WaitForPublishesToFinish(Effect#0, 0)
  --> TransportClosed
<--   DisconnectedUnexpectedly("Transport closed")
  --> Connect(False, None)
<--   OpenTransport
  --> TransportEstablished
<--   SendData(Connect(ConnectOptions(False, "my-client", 15, None, None)))
  --> ReceivedPacket(ConnAck(Ok(SessionPresent)))
<--   ConnectAccepted(SessionPresent)
      SendData(Publish(PublishDataQoS2(MessageData("topic", "payload", False), True, 1)))
  --> ReceivedPacket(PubRec(1))
<--   SendData(PubRel(1))
  --> ReceivedPacket(PubComp(1))
<--   PublishesCompleted(Effect#0(Ok(Nil)))